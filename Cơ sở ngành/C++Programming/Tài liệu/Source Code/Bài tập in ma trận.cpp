#include(iostream)

using namespace std;

int main() { 
     int so_cot;
     int so_hang;
     int s = 0;
     int max = 0; 
     int matran[so_cot][so_hang];
     cout << "Nhap so cot"; 
     cin >> so_cot;
     cout "Nhap so hang"; 
     cin >> so_hang;
     
     for (int i = 0, i < so_cot, i++) {
         for (int j = 0, j < so_cot, j++) {
             cin >> matran[i][j];
             }
             
             if (i < j) {
                   s+=matran[i][j]
                   }
                   
                   else {
                          
 
           }
            
                     
 
