#!/bin/bash
for i in $(seq 1 100); do
    A=$(printf '%03d' "$i")
    USER="student${A}"
    HOME_DIR="/home/${USER}"
    if [ -d "$HOME_DIR" ]; then
        cp -r /home/minhkhanhla/lab/* "$HOME_DIR"/
        chown -R "$USER:$USER" "$HOME_DIR"
        chmod -R a+rx "$HOME_DIR"
    fi
done