#!/bin/bash
if [ "$#" -eq 0 ]; then
  echo "Bạn chưa nhập tham số!"
  exit 1
fi
n="$1"
num=1
for ((i=1; i<=n; i++)); do
  for ((j=1; j<=i; j++)); do
    printf "%d " "$num"
    ((num++))
  done
  echo
done